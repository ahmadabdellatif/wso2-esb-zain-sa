openapi: 3.0.1
info:
  title: RegisterationAPI
  description: API Definition of is
  version: 1.0.0
servers:
- url: http://localhost:8290/zain
paths:
  /signup:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
